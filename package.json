{
  "name": "gen-summary",
  "version": "0.1.0",
  "description": "Parses a SUMMARY.md file into a JSON object that could be written to file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Cardenas (jcardenas9x)",
  "license": "ISC",
  "dependencies": {
    "commander": "^3.0.2"
  }
}
